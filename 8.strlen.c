//using library function strlen

#include <stdio.h>
#include <string.h>
int main( ) {
char str[20] = "BeginnersBook";
int len;
len = strlen(str);
printf("Length of the string is : %d", len);
return 0;
}
